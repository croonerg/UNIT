package no.unit.transformer;

public class Validator {

  public static boolean validate(String string, String schema) /*throws ValidationException*/ {

    // validate xml and json here (refactor)

    return true;
  }
}
